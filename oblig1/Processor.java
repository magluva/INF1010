class Processor {
  // param int coreNum = 4; //Number of processor cores.
  // param double clockSpeed = 3.4 // Frequency in GHz
  int coreNum;
  double clockSpeed;

  public Processor(int cores, double speed) {
    coreNum = cores;
    clockSpeed = speed;
  }

}
